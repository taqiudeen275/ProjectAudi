# AudiLink Admin

The separately deployed, staff-only operational surface for AudiLink. The current implementation is a non-production, zero-backend-dependency interface preview with typed scenario data; authenticated API reads and mutations will replace the fixtures when the control API is available.

```bash
bun run dev:admin
```

The development server uses port 3002 by default.
